package jenkins.data;

/**
 * @author Kohsuke Kawaguchi
 */
public class WriteDataContext extends DataContext {
    public WriteDataContext(DataModelRegistry registry) {
        super(registry);
    }
}
