blurb = Content Security Policy is configured to use the HTTP header <code>{0}</code> based on the system property <code>jenkins.security.csp.impl.Decorator.headerName</code>. \
    It cannot be configured through the UI while this system property specifies a header name.
