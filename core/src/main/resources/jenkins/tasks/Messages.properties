SimpleBuildStep.WorkspaceContextNeeded=This build step requires a workspace context, but none was provided.
SimpleBuildStep.NeedPerformMethodWithoutWorkspace=When marking a build step as not requiring a workspace context, you \
  must implement the overload of the perform() method that does not take a workspace or launcher.