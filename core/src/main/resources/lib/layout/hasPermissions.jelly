<j:jelly xmlns:j="jelly:core" xmlns:d="jelly:define" xmlns:s="jelly:stapler">
  <j:if test="${h.hasPermission(app.ADMINISTER) or h.hasPermission(it.CONFIGURE) or h.hasPermission(it.DELETE)}">
    <d:invokeBody />
  </j:if>
</j:jelly>
