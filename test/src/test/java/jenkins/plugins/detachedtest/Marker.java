package jenkins.plugins.detachedtest;

/**
 * Package anchor for a {@link org.jvnet.hudson.test.junit.jupiter.RealJenkinsExtension.SyntheticPlugin}
 * with no functionality of its own.
 * Used by tests as a stand-in for a synthetic plugin (for example, one with no dependents or declaring an old baseline).
 */
public final class Marker {
    private Marker() {
    }
}
