<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.jenkins-ci.main</groupId>
    <artifactId>jenkins-parent</artifactId>
    <version>${revision}${changelist}</version>
  </parent>

  <artifactId>cli</artifactId>

  <name>Jenkins cli</name>
  <description>Command line interface for Jenkins</description>
  <url>https://github.com/jenkinsci/jenkins</url>

  <properties>
    <mina-sshd.version>2.19.0</mina-sshd.version>
    <!-- Filled in by jacoco-maven-plugin -->
    <jacocoSurefireArgs />
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.jenkins-ci.main</groupId>
        <artifactId>jenkins-bom</artifactId>
        <version>${project.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <dependency>
      <groupId>commons-io</groupId>
      <artifactId>commons-io</artifactId>
      <optional>true</optional>
    </dependency>
    <dependency>
      <groupId>org.apache.sshd</groupId>
      <artifactId>sshd-common</artifactId>
      <version>${mina-sshd.version}</version>
      <optional>true</optional>
    </dependency>
    <dependency>
      <groupId>org.apache.sshd</groupId>
      <artifactId>sshd-core</artifactId>
      <version>${mina-sshd.version}</version>
      <optional>true</optional>
    </dependency>
    <dependency>
      <groupId>org.bouncycastle</groupId>
      <artifactId>bcprov-jdk18on</artifactId>
      <version>1.85.2</version>
      <optional>true</optional>
    </dependency>
    <dependency>
      <groupId>org.glassfish.tyrus.bundles</groupId>
      <artifactId>tyrus-standalone-client-jdk</artifactId>
      <version>2.2.2</version>
      <optional>true</optional>
    </dependency>
    <dependency>
      <groupId>org.jenkins-ci</groupId>
      <artifactId>annotation-indexer</artifactId>
      <optional>true</optional>
    </dependency>
    <dependency>
      <groupId>org.jvnet.localizer</groupId>
      <artifactId>localizer</artifactId>
      <optional>true</optional>
    </dependency>
    <dependency>
      <groupId>org.slf4j</groupId>
      <artifactId>slf4j-jdk14</artifactId>
      <optional>true</optional>
    </dependency>
    <dependency>
      <groupId>com.github.spotbugs</groupId>
      <artifactId>spotbugs-annotations</artifactId>
      <scope>provided</scope>
      <optional>true</optional>
      <exclusions>
        <exclusion>
          <groupId>com.google.code.findbugs</groupId>
          <artifactId>jsr305</artifactId>
        </exclusion>
      </exclusions>
    </dependency>
    <dependency>
      <groupId>org.kohsuke</groupId>
      <artifactId>access-modifier-annotation</artifactId>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <configuration>
          <annotationProcessorPaths>
            <!-- Indexes @Restricted; version resolves from dependencyManagement (jenkins-bom) -->
            <path>
              <groupId>org.jenkins-ci</groupId>
              <artifactId>annotation-indexer</artifactId>
            </path>
          </annotationProcessorPaths>
        </configuration>
      </plugin>
      <plugin>
        <artifactId>maven-surefire-plugin</artifactId>
        <!-- Version specified in grandparent POM -->
        <configuration>
          <argLine>@{jacocoSurefireArgs}</argLine>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-failsafe-plugin</artifactId>
        <version>3.5.6</version>
        <!--
          ShadedJarCryptoIT guards the BouncyCastle excludes in the shade filter below. It has to
          run after package, against the shaded jar: the ordinary test classpath still carries the
          untrimmed bcprov, so a surefire test would pass no matter what the filter removed.
        -->
        <configuration>
          <systemPropertyVariables>
            <shaded.cli.jar>${project.build.directory}/${project.build.finalName}.jar</shaded.cli.jar>
          </systemPropertyVariables>
        </configuration>
        <executions>
          <execution>
            <goals>
              <goal>integration-test</goal>
              <goal>verify</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-shade-plugin</artifactId>
        <version>3.6.2</version>
        <executions>
          <execution>
            <goals>
              <goal>shade</goal>
            </goals>
            <phase>package</phase>
            <configuration>
              <createDependencyReducedPom>false</createDependencyReducedPom>
              <relocations>
                <relocation>
                  <pattern>org</pattern>
                  <shadedPattern>io.jenkins.cli.shaded.org</shadedPattern>
                </relocation>
                <relocation>
                  <pattern>net</pattern>
                  <shadedPattern>io.jenkins.cli.shaded.net</shadedPattern>
                </relocation>
                <relocation>
                  <pattern>com</pattern>
                  <shadedPattern>io.jenkins.cli.shaded.com</shadedPattern>
                </relocation>
                <relocation>
                  <pattern>jakarta</pattern>
                  <shadedPattern>io.jenkins.cli.shaded.jakarta</shadedPattern>
                </relocation>
              </relocations>
              <transformers>
                <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                  <mainClass>hudson.cli.CLI</mainClass>
                  <manifestEntries>
                    <Jenkins-CLI-Version>${project.version}</Jenkins-CLI-Version>
                  </manifestEntries>
                </transformer>
                <transformer implementation="org.apache.maven.plugins.shade.resource.ServicesResourceTransformer" />
              </transformers>
              <filters>
                <filter>
                  <!-- signature might cause some trouble -->
                  <artifact>*:*</artifact>
                  <excludes>
                    <exclude>META-INF/*.SF</exclude>
                    <exclude>META-INF/*.DSA</exclude>
                    <exclude>META-INF/*.RSA</exclude>
                    <!--
                      Multi-release variants are dead code in this uber jar: the shaded manifest does not declare
                      Multi-Release: true and the versioned classes are not relocated, so the JVM can never load
                      them. BouncyCastle et al. run on their base classes.
                    -->
                    <exclude>META-INF/versions/**</exclude>
                    <!--
                      annotation-indexer declares an annotation processor. Relocating it leaves this
                      uber jar advertising io.jenkins.cli.shaded...AnnotationProcessorImpl, so javac
                      runs it in every module that compiles against the CLI. It can never match
                      anything, since the annotations it looks for are relocated too, but it does
                      defeat compile avoidance. A runtime CLI jar has no compile-time processor.
                    -->
                    <exclude>META-INF/services/javax.annotation.processing.Processor</exclude>
                  </excludes>
                </filter>
                <filter>
                  <!--
                    BouncyCastle is here only for Apache MINA SSHD, which negotiates post-quantum
                    key exchange by default. Everything sshd can negotiate is kept: of the pqc tree
                    it names only pqc/crypto/ntruprime (sntrup761), and ML-KEM lives in
                    crypto/{params,kems,generators}, outside pqc. The families excluded below are
                    ones sshd never asks for. They are not obsolete crypto, just unreachable here.

                    Two things to keep in mind when bumping mina-sshd or bcprov:
                      - Revisit this list. If sshd gains post-quantum host key signatures it will
                        need mldsa/slhdsa/falcon back. CliCryptoTest asserts that every key
                        exchange sshd offers by default is still supported, so a miss fails the
                        build rather than silently downgrading the connection. sshd swallows the
                        NoClassDefFoundError and just reports isSupported() == false.
                      - Do not exclude pqc/jcajce. BouncyCastleProvider's constructor calls
                        loadPQCKeys(), which does a hard "new" on every KeyFactorySpi there, so
                        removing it stops the provider registering and takes Ed25519 with it. The
                        algorithm implementations behind those shells resolve lazily and are the
                        part that is safe to drop.

                    Paths are pre-relocation, since filters are applied as source artifacts are read.
                  -->
                  <artifact>org.bouncycastle:bcprov-jdk18on</artifact>
                  <excludes>
                    <!-- Deprecated by BouncyCastle itself: Rainbow was broken in 2022, Picnic did
                         not survive NIST selection. Picnic alone is over a megabyte. -->
                    <exclude>org/bouncycastle/pqc/legacy/**</exclude>
                    <!-- Signature and KEM families sshd never asks for. -->
                    <exclude>org/bouncycastle/pqc/crypto/aimer/**</exclude>
                    <exclude>org/bouncycastle/pqc/crypto/cmce/**</exclude>
                    <exclude>org/bouncycastle/pqc/crypto/faest/**</exclude>
                    <exclude>org/bouncycastle/pqc/crypto/falcon/**</exclude>
                    <exclude>org/bouncycastle/pqc/crypto/frodo/**</exclude>
                    <exclude>org/bouncycastle/pqc/crypto/haetae/**</exclude>
                    <exclude>org/bouncycastle/pqc/crypto/hawk/**</exclude>
                    <exclude>org/bouncycastle/pqc/crypto/hqc/**</exclude>
                    <exclude>org/bouncycastle/pqc/crypto/lms/**</exclude>
                    <exclude>org/bouncycastle/pqc/crypto/mayo/**</exclude>
                    <exclude>org/bouncycastle/pqc/crypto/mldsa/**</exclude>
                    <exclude>org/bouncycastle/pqc/crypto/mqom/**</exclude>
                    <exclude>org/bouncycastle/pqc/crypto/newhope/**</exclude>
                    <exclude>org/bouncycastle/pqc/crypto/ntru/**</exclude>
                    <exclude>org/bouncycastle/pqc/crypto/ntruplus/**</exclude>
                    <exclude>org/bouncycastle/pqc/crypto/qruov/**</exclude>
                    <exclude>org/bouncycastle/pqc/crypto/saber/**</exclude>
                    <exclude>org/bouncycastle/pqc/crypto/sdith/**</exclude>
                    <exclude>org/bouncycastle/pqc/crypto/slhdsa/**</exclude>
                    <exclude>org/bouncycastle/pqc/crypto/snova/**</exclude>
                    <exclude>org/bouncycastle/pqc/crypto/sphincs/**</exclude>
                    <exclude>org/bouncycastle/pqc/crypto/sqisign/**</exclude>
                    <exclude>org/bouncycastle/pqc/crypto/uov/**</exclude>
                    <exclude>org/bouncycastle/pqc/crypto/xmss/**</exclude>
                    <exclude>org/bouncycastle/pqc/crypto/xwing/**</exclude>
                  </excludes>
                </filter>
              </filters>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.jvnet.localizer</groupId>
        <artifactId>localizer-maven-plugin</artifactId>
        <!-- version specified in grandparent pom -->
        <executions>
          <execution>
            <goals>
              <goal>generate</goal>
            </goals>
            <configuration>
              <fileMask>Messages.properties</fileMask>
              <outputDirectory>target/generated-sources/localizer</outputDirectory>
              <accessModifierAnnotations>true</accessModifierAnnotations>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>build-helper-maven-plugin</artifactId>
        <!-- Version specified in grandparent POM -->
        <executions>
          <execution>
            <id>add-source</id>
            <goals>
              <goal>add-source</goal>
            </goals>
            <phase>generate-sources</phase>
            <configuration>
              <sources>
                <source>${project.build.directory}/generated-sources/localizer</source>
              </sources>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
  <profiles>
    <profile>
      <id>enable-jacoco</id>
      <build>
        <plugins>
          <plugin>
            <groupId>org.jacoco</groupId>
            <artifactId>jacoco-maven-plugin</artifactId>
            <!-- Version specified in parent POM -->
            <executions>
              <execution>
                <goals>
                  <goal>prepare-agent</goal>
                </goals>
                <configuration>
                  <propertyName>jacocoSurefireArgs</propertyName>
                  <includes>
                    <include>hudson/*</include>
                    <include>hudson/**/*</include>
                    <include>jenkins/*</include>
                    <include>jenkins/**/*</include>
                    <include>org/jenkins/**/*</include>
                  </includes>
                  <excludes>
                    <exclude>**/Messages.class</exclude>
                  </excludes>
                </configuration>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>
</project>
